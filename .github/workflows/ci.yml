name: "CI"
on: [push, pull_request]
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: Setup BATS
        uses: mig4/setup-bats@v1

      - name: Check out code
        uses: actions/checkout@v2

      - name: Test
        run: bats test/
